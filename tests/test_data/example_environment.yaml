# Example YAML specifying an environment
robot_base_poses:
  spot: [0, 0, 0, 0, 0, 0] # x, y, z, roll, pitch, yaw

object_poses:
  table1: [3, 0, 0, 0, 0, 0]
  bottle1:
    xyz_rpy: [0, 0, 1, 0, 0, 0] # Above the table
    frame: table1

waypoints:
  past_table: [3.5, 0, 3.14159]

collision_models:
  table1:
    meshes:
      - filepath: meshes/table.glb
        transforms:
          - center_bounds
          - bottom_at_zero_z

  bottle1:
    primitives:
      - { type: cylinder, height: 0.15, radius: 0.03 } # Tall, thin cylinder
      - { type: cylinder, height: 0.9, radius: 0.05 } # Short, wide cylinder

default_frame: map
